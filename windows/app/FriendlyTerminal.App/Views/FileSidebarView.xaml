<UserControl
    x:Class="FriendlyTerminal.App.Views.FileSidebarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="12,8" ColumnSpacing="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold" Opacity="0.7" Text="Files" />
            <Button x:Name="HiddenToggle" Grid.Column="1" Click="OnToggleHidden" Padding="6" ToolTipService.ToolTip="Show or hide hidden files">
                <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" FontSize="12" Glyph="&#xE890;" />
            </Button>
        </Grid>

        <ListView
            x:Name="FileList"
            Grid.Row="1"
            Padding="4"
            IsItemClickEnabled="True"
            ItemClick="OnItemClick"
            RightTapped="OnItemRightTapped"
            SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="2,1" />
                    <Setter Property="MinHeight" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10,4" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <FontIcon Grid.Column="0" FontSize="14" FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="{Binding Glyph}" />
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="12" Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="2" VerticalAlignment="Center" FontSize="10" Opacity="0.55" Text="{Binding SizeDisplay}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border Grid.Row="2" Background="{ThemeResource ControlFillColorSecondary}">
            <TextBlock x:Name="FooterText" Padding="12,5" FontSize="11" Opacity="0.6" />
        </Border>
    </Grid>
</UserControl>
